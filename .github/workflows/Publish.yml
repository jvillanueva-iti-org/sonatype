name: Publish

on:
  push:
    branches:
    - main
jobs:
  publish:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: Import GPG Owner Trust
      run: RTgxNDVGRjY5ODhCNzcxRjRDQjk4MDA3OTU5QzI2RDNDOEREM0QzMzo2Og== | base64 --decode | gpg --import-ownertrust

    #- name: Import GPG key
    #  run: echo ${{secrets.GPG_SECRET_KEYS}} | base64 --decode | gpg --import --no-tty --batch --yes

    #- name: Clean settings.xml
    #  run: rm -rf ~/.m2/settings.xml

    #- name: Create settings.xml
    #  uses: s4u/maven-settings-action@v1
    #  with:
    #    servers: '[{"id": "ossrh", "username": "${{secrets.SONATYPE_USERNAME}}", "password": "${{secrets.SONATYPE_PWD}}"}]'
